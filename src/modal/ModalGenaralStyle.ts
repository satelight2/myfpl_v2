import {StyleSheet} from 'react-native';

const ModalGenaralStyle = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    padding: 10,
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
  },
});
export default ModalGenaralStyle;
